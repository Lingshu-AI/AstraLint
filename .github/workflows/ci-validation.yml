#
# Copyright 2024-2026 the original author or authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: ✅ CI Validation
on:
  workflow_dispatch: # Allow manual trigger
  schedule:
    - cron: "0 2 * * 1" # Run every Monday at 2:00 UTC
permissions:
  contents: read
jobs:
  validate-ci-config:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - name: Validate GitHub Actions Workflows
        run: |
          echo "🔍 Validating GitHub Actions workflows..."

          # Check if all required workflow files exist
          required_workflows=(
            ".github/workflows/build-and-test.yml"
            ".github/workflows/code-quality.yml" 
            ".github/workflows/security.yml"
            ".github/workflows/deploy.yml"
          )

          for workflow in "${required_workflows[@]}"; do
            if [[ -f "$workflow" ]]; then
              echo "✅ Found: $workflow"
            else
              echo "❌ Missing: $workflow"
              exit 1
            fi
          done

          # Check if Makefile exists and has required targets
          if [[ -f "Makefile" ]]; then
            echo "✅ Found: Makefile"
            
            required_targets=("build" "test" "quality" "security-check" "docker-build")
            for target in "${required_targets[@]}"; do
              if grep -q "^$target:" Makefile; then
                echo "✅ Found Makefile target: $target"
              else
                echo "❌ Missing Makefile target: $target"
                exit 1
              fi
            done
          else
            echo "❌ Missing: Makefile"
            exit 1
          fi

          echo "🎉 All CI configuration files are present!"

      - uses: ./tools/github-actions/setup-deps
      - name: Validate Maven Configuration
        run: |
          echo "🔍 Validating Maven configuration..."

                      # Check if pom.xml has required plugins
            required_plugins=(
              "maven-checkstyle-plugin"
              "jacoco-maven-plugin"
            )

          for plugin in "${required_plugins[@]}"; do
            if grep -q "$plugin" pom.xml; then
              echo "✅ Found Maven plugin: $plugin"
            else
              echo "❌ Missing Maven plugin: $plugin"
              exit 1
            fi
          done

          # Validate Maven compilation
          mvn clean compile -q
          echo "✅ Maven compilation successful"

          echo "🎉 Maven configuration is valid!"
